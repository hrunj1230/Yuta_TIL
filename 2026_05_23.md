# 수학의 구조
현대 수학의 가장 기본 구조 : 집합
$$
A = {1,2,3}
$$

집합은 원소 object가 모인것 이다 .
object: 객체라고 번역하는 경우는 프로그래밍이 거의 유일
수학적 대상
모여있다 => 구조 => list tuple set dictionary = cs data structure 자료구조

중간 정리
집합
곱집합
	몫집합
	함수[^1]
	
	
	
실수는 유리수의 수열 

# 벡터공간

가군(module)
# 집합 1개에 대한 구조
군: 연산 1개
환,체 :연산 2개 (덧셈,곱셈)

# 집합 2개에 대한 구조

$$
\mathbb{R}^n \implies dim\mathbb{R}^n = n \\
(: x, y :)
$$

유클리드 공간에서 표준 기저에서 표준내적(표준 놈 표준거리) 으로 대상을 다룬다.