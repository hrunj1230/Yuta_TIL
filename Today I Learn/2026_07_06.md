
양자는 최소 양(amount)이다
모델 양자는 모델을 묘사하는 최소의 양 - 1

affine Quantization
affine 선형 변환에 평행이동이 더해진 아핀변환

모델 로딩(loading)
- 모델을 내려받는다.(F16)
- 모델의 가중치를 파일에서 비디오 메모리로 올린다.(int4)

activation Quantization
활성화함수를 정수로 양자화

w8a8/w4a16

calibration

outlier problem
LLM텐서에 존재하는 소수의 극단값이 양자화의 표현 해상도를 잠식해 저비트 정확도가 급락하는 현상

GPTQ
이미 학습된 LLM의 가중치를 INT4같은 낮은 비트로 줄이되, 단순 반올림보다 출력 품질이 덜 떨어지도록 보정하는 PTQ기법

hessian 헤세 행렬 (헤시안) - 다변수 함수의 이계도 함수 (극대 극소)
야코비안 행렬 -다변수 벡터 함수의 도함수 행렬


AWQ
활성화 통계를 보고 출력에 중요한 가중치 채널을 찾아, 해당 채널이 INT4같은 저비트 양자화에서 덜 손상되도록 스케일을 조정하는 PTQ기법



선형 비선형 확률