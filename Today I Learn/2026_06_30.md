## Checkpointer
체크포인트는 인터페이스다.
인터페이스 -> 구현 (implementation)
체크포인터 인터페이스를 구현한 구현체가 다수 존재한다.

Thread

Checkpoint 는 스냅샷이다.

time travel 
디버깅 문제 발생 문제 발견
문제 재현 reproduction

durable Execution 
장기 실행
Determinism
Idempotence 멱등성
여러번 불러도 반환값이 똑같다.

side effect 부수효과
main effect
함수의 주(메인) 효과와 부수 효과
주 함수 : 반환값
부수 효과 : 반환 값이 아닌것? 반환값이 아닌 모든것





ai 챗봇 캐쉬? 많이 쓰나? 프롬프트?
ai캐싱

프롬프트 캐싱
- 키워드 기반 캐싱 - 정확하게 동일한 프롬프트가 들어왔을때 
- 의미 기반 캐싱 - 상당히 동작 
컨텍스트 캐싱 - RAG수준에서의 캐싱
 - 문서 검색 : 캐싱
여러가지 퀘스트? ㅋㅋ

모델 공개 가중치
vLLM 없이 준다
GPU서버를 쓸수 있어야 한다.
