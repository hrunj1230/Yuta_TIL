
## LangGraph
- 상태를 가진 LLM 애플리케이션의 워크플로와 에이전트를 그래프 구조로 설계하고 실행하는 LangChain 팀의 저수준 오케스트레이션 프레임워크
- 그래프 구조로 워크 플로를 설계하는 에이전트 프레임워크다.
- (에이전트 프레임워크다)
- 파이프 라인을 설계한다 (실행 흐름을 미리 그래프로 설계)

## Builder
상태 스키마: state class 상태구조

## workflow
워크플로우는 수식어 구조다.
워크 플로우는 단계적으로 상태를 처리하는 앱 구조다.

chain 직선구조
graph 그래프 구조
workflow 단계 순서 구조

## AI Agent
에이전트는 AI를 써서 도구 호출 등의 행동을 하게 하는 구조이다.

## Graph API
Graph API는 그래프를 구축하는 랭 그래프의 기본 API다
상태 스키마, 노드,엣지를 통해 그래프를 구축하는 랭그래프의 기본 API다.

## StateGraph
그래프 실행 시점의 현재 스냅샷(순간?)을 담는 공유 데이터 구조
StateGraph는 billder class 다.
그래프를 만들어내는 클래스다.

Graph State Schema

Graph State Schema는 정의다. 상태 구조의 
## Reducer
Reducer 함수다
합치는 로직을 담은 함수
기존 값과 새 값을 합치는 로직을 담은 함수다.
## MessagesState
MessagesState 는 상태 스키마다.
MessagesState는 랭그래프 기본 상태 스키마다.
MessagesState는 메시지를 누적하는 랭그래프 기본 상태 스키마다.

## Node
node는 함수다.
node 는 상태를 입력받아 갱신할 부분을 출력하는 함수다
node 는 상태를 입력받아 작업을 수행하고 생신할 부분을 출력하는 함수다

## Edge
Edge는 랭그래프에서 한 노드의 실행이 끝난 뒤 다음에 실행할 노드 또는 종료 지점을 지정하는 그래프 연결
Edge는 그래프 연결이다.
Edge는 노드 실행을 표현하는 유향(directed) 그래프 연결이다.

## Graph Visualization
Graph Visualization 

## Tool Calling 
tool calling 은 기능이다.
tool calling 은 도구의 호출을 판단하는 기능이다.
tool calling 은 사용자 입력의 분석 결과로 도구의 호출을 판단하는 기능이다.

## Tool Node
tool node는 사전 구성 노드다.
tool node는 LLM이 판단한 도구 선택을 바탕으로 실제 수행하여 결과 메시지를 만드는 사전 구성 노드다.

## Routing
라우팅은 디자인(설계) 패턴이다.
라우팅은 입력 또는 상태에 기반하여 다음 실행 노드 선택 디자인 패턴이다.

상태 스키마(class) -> node (함수) -> routing(함수)
그래프 빌더 ( 스키마 ) -> 노드 추가 -> 엣지추가 -> 컴파일

## Parallelization 
병렬화는 (디자인) 패턴이다.
Parallelization 는 병렬 실행을 위한 패턴이다.
Parallelization 는 하나의 노드로부터 여러 노드에 연결되어 동시에 실행된 후 모든 노드의 결과를 취합하는 병렬 실행 패턴이다.
sk : 여러 노드로 하나의 노드를 연결하여 모든 결과를 취합하는 병렬 실행 패턴이다.

직렬로 처리하면 너무 느릴떄가 있다.
정적크기로 분할 병렬화 처리
## Send
send는 객체다
send는 동적 분기 객체다.
send는 병렬 실행을 하는 동적 분기 객체다.
런타임(실행하는 중)에 생성된 데이터 목록을 기준으로 동일한 노드를 여러번 병렬 실행 하도록 지시하는 동적 분기 객체
## Command
command는 객체다
command는 랭그래프 객체다.
command는 상태 갱신과 다음 노드 지정을 할 수 있는 랭그래프 객체다.

## Loop
loop 는 구조다.
loop은 랭 그래프 구조다.
loop는 종료 조건이 만족될 때까지 반복하는 랭그래프 구조다.
종료조건이 아예없다 = 무한루프 
무한루프 = 절대악? ㅋㅋㅋ

## Loop Termination

## ReAct Pattern
(Re = Reasoning 추론)
리액트 패턴은 구조다.
리액트 패턴은 에이전트의 실행 구조다.
리액트 패턴은 LLM이 추론하고 실행한다음 결과를 바탕으로 반복하는 에이전트 실행 구조다.

판단 + 행동 에이전트 - > AX AI transform
(그러면 판단은 자기자긴 llm이 하나?)
## Functional API
Functional api는 api다
함수형 api는 함수 호출 흐름을 워크 플로우로 실행하는 api다.









chain = 
retriever | prompt | model | parser
직선적 흐름 (serial)


6주 : 바닐라 RAG
7주 : LangChain RAG
8주 : LangGraph RAG

라우팅 패턴을 LLM으로 많이 쓴다. LLM라우팅도 도전해보자

라우팅 패턴 : 라우팅 함수를 조건부 엣지에 등록
라우터 노드 : 커맨드 객체를 반환하는 함수를 노드로 등록 

위클리 챌린지 : 개인 프로젝트의 랭체인 기반을 랭그래프 기반으로 바꾼다.
