python
oop
framework :한가지 길을 안내

class
함수
메소드 
self 자기자신 자기참조

data class 
@데코레이터 - @ : 앳(at)읽기
필수? 아니고
추가적인 정보를 준다 (메타데이터)
ai - 통계 /데이터
ai 에서는 데이터 클래스 중요
속성관리 중요 (노션에 있음)

concurrency(동시성)
실행
코드작성: 명령하달 - 시나리오
파이썬 실행기 ( 런타임 ,인터프리터)
OS 실제로는 cpu는 한번에 하나의 명령만 처리하지만 작업을 매우 빠르게 번갈아 수행하기 떄문에 동시에 진행되는것처럼 보인다. ex)동영상같이
(지금은 멀티코어라 아니지만 코어 기준에서는 맞다)
멀티스레드
멀티프로세스
비동기 프로그래밍

GIL 한 시점에서 하나의 스레드만 파이썬 바이트코드를 실행하도록 제한하는 lock 락
GIL 로 인해서 CPU바운드 작업(연산작업 등)에서는 스레드가 동시에 실행되지 못하고 번갈아 실행됩니다.
안정성 보장


thread -- os 에서 프로세스랑 스레드 공부를 해야 어떻게 동작하는지 이해해야 한다
그럼에도 스레드 작업을 하는 이유는  I/O-bound
input()/print()
cpu작업이 아닐떄 그 스레드를 멈추고 다른 I/O같은 작동을 실행할떈 멈추고 다른 스레드를 켜서 작업

process(모듈)
여러 코어를 활용하기 위해 ( 멀티코어)
AWS,GCP,colab 서버 코어수가 적다
클라우드 환경은 코어가 가상코어다 vCore
cpu 위에 가상화 솔루션을 올린다 -그래서 생각보다 성능이 떨어진다.

async(비동기)에이싱크
(스레드 작업의 최신버전 ㅋㅋ)
async / await ( 타동사)
await (io바운드?)
지금 cli프로그램에 비동기 처리가 가능해보이는 것을 리팩토링 async await 을 적용해보자
네트워킹 (api 호출)
vLLM

this week
git - 기본 마스터 
파이썬 응용 - 예외처리 이터레이터 제너레이터 파일 I/O
는 꼭 이번주에 하고 넘어가기

next week
fastAPI (웹 기초)
데이터베이스 기초 