export interface AiSimulationReport {
  id: number; // PK / BIGINT
  session_id: number; // FK / BIGINT
  status: string; // VARCHAR(20)
  overall_score: number | null; // DECIMAL(5,2)
  summary: string | null; // TEXT
  conversation_flow_grade: string | null; // CHAR(1)
  question_exchange_grade: string | null; // CHAR(1)
  interest_expression_grade: string | null; // CHAR(1)
  initiative_balance_grade: string | null; // CHAR(1)
  detail_result: any | null; // JSON
  created_at: Date; // DATETIME(6)
  completed_at: Date | null; // DATETIME(6)
}

export interface ProfileImage {
  id: number; // PK / BIGINT
  profile_id: number; // FK / BIGINT
  image_key: string; // VARCHAR(500)
  display_order: number; // SMALLINT
  is_frontal: boolean; // BOOLEAN
  created_at: Date; // DATETIME(6)
  deleted_at: Date | null; // DATETIME(6)
}

export interface SajuAnalysisResult {
  id: number; // PK / BIGINT
  saju_analysis_id: number; // FK / BIGINT
  summary: string; // TEXT
  detail_result: any; // JSON
  created_at: Date; // DATETIME(6)
  deleted_at: Date | null; // DATETIME(6)
}

export interface MemberAuthAccount {
  id: number; // PK / AUTO_INCREMENT / BIGINT
  provider: string; // VARCHAR(255)
  member_id: number; // FK / BIGINT
  provider_user_id: string; // VARCHAR(255)
  linked_at: Date; // DATETIME(6)
}

export interface Notification {
  id: number; // PK / AUTO_INCREMENT / BIGINT
  member_id: number; // FK / BIGINT
  type: string; // VARCHAR(30)
  title: string; // VARCHAR(100)
  body: string; // VARCHAR(500)
  target_type: string | null; // VARCHAR(30)
  target_id: number | null; // BIGINT
  read_at: Date | null; // DATETIME(6)
  created_at: Date; // DATETIME(6)
  deleted_at: Date | null; // DATETIME(6)
}

export interface Persona {
  id: number; // PK / AUTO_INCREMENT / BIGINT
  member_id: number; // FK / BIGINT
  system_prompt: string; // TEXT
  personality_summary: any | null; // JSON
  built_at: Date; // DATETIME(6)
}

export interface FaceRecommendationBatch {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  face_analysis_result_id: number; // FK / BIGINT
  created_at: Date; // DATETIME(6)
}

export interface FaceRecommendationItem {
  id: number; // PK / BIGINT
  face_recommendation_id: number; // FK / BIGINT
  target_member_id: number; // FK / BIGINT
  rank_no: number; // INT
}

export interface Like {
  id: number; // PK / BIGINT
  sender_member_id: number; // FK / BIGINT
  receiver_member_id: number; // FK / BIGINT
  status: string; // PENDING, REJECTED, MATCHED / VARCHAR(20)
  created_at: Date; // DATETIME(6)
  resolved_at: Date | null; // 관심없음, 매치 처리로 대기 상태가 끝난 시각 / DATETIME(6)
}

export interface ActivityRegion {
  id: number; // PK / BIGINT
  region_code: string; // VARCHAR(30)
  province_name: string; // VARCHAR(50)
  region_name: string; // VARCHAR(50)
  representative_latitude: number; // DECIMAL(9,6)
  representative_longitude: number; // DECIMAL(9,6)
  created_at: Date; // DATETIME(6)
  updated_at: Date; // DATETIME(6)
}

export interface MemberTermAgreement {
  id: number; // PK / BIGINT
  term_id: number; // FK / BIGINT
  member_id: number; // FK / BIGINT
  agreed_at: Date; // DATETIME(6)
}

export interface SajuRecommendationBatch {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  saju_analysis_result_id: number; // FK / BIGINT
  created_at: Date; // DATETIME(6)
}

export interface MemberBlock {
  id: number; // PK / BIGINT
  blocker_member_id: number; // FK / BIGINT
  blocked_member_id: number; // FK / BIGINT
  blocked_at: Date; // DATETIME(6)
  unblocked_at: Date | null; // DATETIME(6)
}

export interface ChatMessage {
  id: number; // PK / BIGINT
  chat_room_id: number; // FK / match의 식별자 / BIGINT
  sender_member_id: number; // FK / BIGINT
  delegation_id: number | null; // FK / BIGINT
  message_type: string; // TEXT, IMAGE / VARCHAR(30)
  text_content: string | null; // VARCHAR(1000)
  status: string; // SENT, FAILED / VARCHAR(20)
  sequence_no: number; // BIGINT
  created_at: Date; // DATETIME(6)
  sender_deleted_at: Date | null; // DATETIME(6)
  receiver_deleted_at: Date | null; // DATETIME(6)
}

export interface Match {
  id: number; // PK / AUTO_INCREMENT / BIGINT
  member1_id: number; // FK / BIGINT
  member2_id: number; // FK / BIGINT
  status: string; // ACTIVE, ENDED / VARCHAR(20)
  matched_at: Date; // 상호 좋아요로 Match 성립한 시각 / DATETIME(6)
  ended_at: Date | null; // 차단 등의 사유로 Match 종료된 시각 / DATETIME(6)
  end_reason: string | null; // BLOCKED, MEMBER_WITHDRAWN, MEMBER_LEFT_CHAT / VARCHAR(30)
}

export interface FaceCompatibility {
  id: number; // PK / BIGINT
  member_result_id: number; // FK / BIGINT
  target_result_id: number; // FK / BIGINT
  score: number; // DECIMAL(5,2)
  detail_report: any; // JSON
  created_at: Date; // DATETIME(6)
  deleted_at: Date | null; // DATETIME(6)
}

export interface PersonaTrait {
  id: number; // PK / BIGINT
  persona_id: number; // FK / BIGINT
  category: string; // VARCHAR(30)
  label: string; // VARCHAR(20)
  observation_count: number; // SMALLINT
  deleted_at: Date | null; // DATETIME(6)
}

export interface RecommendationBatch {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  generation_type: string; // VARCHAR(20)
  created_at: Date; // DATETIME(6)
}

export interface AiSimulationMessage {
  id: number; // PK / BIGINT
  session_id: number; // FK / BIGINT
  content: any; // JSON
  created_at: Date; // DATETIME(6)
}

export interface Profile {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  activity_region_id: number | null; // FK / BIGINT
  nickname: string | null; // VARCHAR(10)
  height: number | null; // SMALLINT
  body_type: string | null; // VARCHAR(30)
  education_level: string | null; // VARCHAR(30)
  job: string | null; // VARCHAR(50)
  religion: string | null; // VARCHAR(30)
  mbti: string | null; // VARCHAR(4)
  drinking: string | null; // VARCHAR(30)
  smoking: string | null; // VARCHAR(30)
  created_at: Date; // DATETIME(6)
  updated_at: Date; // DATETIME(6)
}

export interface SajuAnalysis {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  input_snapshot: any; // JSON
  status: string; // VARCHAR(20)
  is_retry: boolean; // BOOLEAN
  created_at: Date; // DATETIME(6)
  finished_at: Date | null; // DATETIME(6)
}

export interface FaceAnalysisResult {
  id: number; // PK / BIGINT
  face_analysis_id: number; // FK / BIGINT
  summary: string; // TEXT
  created_at: Date; // DATETIME(6)
  deleted_at: Date | null; // DATETIME(6)
}

export interface ChatMessageImage {
  id: number; // PK / BIGINT
  chat_message_id: number; // FK / BIGINT
  image_key: string; // VARCHAR(500)
  mime_type: string; // VARCHAR(50)
  file_size: number; // BIGINT
  sort_order: number; // TINYINT
}

export interface ChatDelegation {
  id: number; // PK / BIGINT
  requester_member_id: number; // FK / BIGINT
  status: string; // IN_PROGRESS, COMPLETED, FAILED / VARCHAR(20)
  consented_at: Date; // DATETIME(6)
  ended_at: Date | null; // DATETIME(6)
  end_reason: string | null; // 상대 이탈, 위임 종료 등 종료 원인 / VARCHAR(30)
}

export interface Member {
  id: number; // PK / AUTO_INCREMENT / BIGINT
  status: string; // VARCHAR(20)
  birth_date: Date; // DATE
  sex: string; // ENUM('FEMALE', 'MALE')
  name: string; // VARCHAR(100)
  face_verification_status: string; // VARCHAR(20)
  created_at: Date; // DATETIME(6)
  last_accessed_at: Date; // DATETIME(6)
  withdrawn_at: Date | null; // DATETIME(6)
}

export interface Term {
  id: number; // PK / AUTO_INCREMENT / BIGINT
  code: string; // VARCHAR(50)
  title: string; // VARCHAR(150)
  content: string; // TEXT
  is_required: boolean; // BOOLEAN
  created_at: Date; // DATETIME(6)
  ended_at: Date | null; // DATETIME(6)
}

export interface RecommendationPreference {
  member_id: number; // FK / BIGINT
  min_age: number | null; // SMALLINT
  max_age: number | null; // SMALLINT
  min_height: number | null; // SMALLINT
  max_height: number | null; // SMALLINT
  relationship_preference: string | null; // VARCHAR(30)
  religion: string | null; // VARCHAR(30)
  drinking: string | null; // VARCHAR(30)
  smoking: string | null; // VARCHAR(30)
  created_at: Date; // DATETIME(6)
  updated_at: Date; // DATETIME(6)
}

export interface AiPracticeSession {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  target_persona_id: number; // FK / BIGINT
  status: string; // ACTIVE, ENDED / VARCHAR(20)
  started_at: Date; // DATETIME(6)
  ended_at: Date | null; // DATETIME(6)
}

export interface SajuRecommendationItem {
  id: number; // PK / BIGINT
  saju_recommendation_id: number; // FK / BIGINT
  target_member_id: number; // FK / BIGINT
  compatibility_score: number; // DECIMAL(5,2)
  rank_no: number; // INT
}

export interface AiPracticeChat {
  id: number; // PK / BIGINT
  session_id: number; // FK / BIGINT
  user_message: string; // VARCHAR(500)
  ai_response: string | null; // TEXT
  status: string; // GENERATING, COMPLETED, FAILED / VARCHAR(20)
  is_retry: boolean; // BOOLEAN
  created_at: Date; // DATETIME(6)
  completed_at: Date | null; // DATETIME(6)
}

export interface ChatRoom {
  id: number; // FK / match의 식별자 / BIGINT
  status: string; // ACTIVE, ENDED, INACTIVE / VARCHAR(20)
  created_at: Date; // DATETIME(6)
  ended_at: Date | null; // 메시지 전송이 불가능해진 시각 / DATETIME(6)
  end_reason: string | null; // 차단, 상대 이탈, 탈퇴, AI 위임 / VARCHAR(30)
}

export interface RecommendationPass {
  id: number; // PK / BIGINT
  passer_member_id: number; // FK / BIGINT
  passed_member_id: number; // FK / BIGINT
  created_at: Date; // DATETIME(6)
}

export interface ChatParticipant {
  id: number; // PK / AUTO_INCREMENT / BIGINT
  chat_room_id: number; // FK / match의 식별자 / BIGINT
  member_id: number; // FK / BIGINT
  status: string; // ACTIVE, LEFT / VARCHAR(20)
  push_enabled: boolean; // BOOLEAN
  left_at: Date | null; // DATETIME(6)
  last_read_sequence_no: Date; // DATETIME(6)
}

export interface SajuCompatibility {
  id: number; // PK / BIGINT
  requester_result_id: number; // FK / BIGINT
  target_result_id: number; // FK / BIGINT
  score: number; // DECIMAL(5,2)
  detail_result: any; // JSON
  created_at: Date; // DATETIME(6)
  deleted_at: Date | null; // DATETIME(6)
}

export interface RecommendationItem {
  id: number; // PK / BIGINT
  recommendation_batch_id: number; // FK / BIGINT
  candidate_member_id: number; // FK / BIGINT
  ranking_order: number; // INTEGER
}

export interface SajuProfile {
  member_id: number; // FK / BIGINT
  birth_time: string | null; // VARCHAR(20)
  updated_at: Date; // DATETIME(6)
}

export interface PersonaBuild {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  build_type: string; // VARCHAR(30)
  status: string; // VARCHAR(20)
  input_data: any; // JSON
  source_from_at: Date | null; // DATETIME(6)
  source_to_at: Date | null; // DATETIME(6)
  started_at: Date; // DATETIME(6)
  finished_at: Date | null; // DATETIME(6)
}

export interface AiSimulationSession {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  my_persona_id: number; // FK / BIGINT
  target_persona_id: number; // FK / BIGINT
  status: string; // VARCHAR(20)
  created_at: Date; // DATETIME(6)
  finished_at: Date | null; // DATETIME(6)
}

export interface FaceAnalysis {
  id: number; // PK / BIGINT
  member_id: number; // FK / BIGINT
  status: string; // PROCESSING, COMPLETED, FAILED, CANCELED / VARCHAR(20)
  is_retry: boolean; // BOOLEAN
  created_at: Date; // DATETIME(6)
  finished_at: Date | null; // 완료·실패·취소된 시각 / DATETIME(6)
}
