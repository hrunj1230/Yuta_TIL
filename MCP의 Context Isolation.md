MCP의 Context Isolation이 AI Agent와 외부 도구·데이터 소스를 연결할 때 필요한 이유를 설명하고, 컨텍스트가 분리되지 않았을 때 발생할 수 있는 보안·권한·데이터 오염 문제를 구체적으로 분석하시오.

### MCP의 Context Isolation이 

mcp - ai에이전트가 여러 외부 도구와 데이터 소스에 연결하도록 
context isolation ( 컨텍스트 격리 ) - 작업이나 요청이 서로의 context (문맥) 에 영향을 받지 않도록 분리/격리 하는 것
### AI Agent와 외부 도구·데이터 소스를 연결할 때 필요한 이유를 설명하고,

0. LLM의 평면적인 의미공간(semantic space) 아키텍쳐이기 때문이다.고차원의 벡터에 한 점으로 계층적이지 않고 거리와 방향의 관계만 있다.
1. trust boundary(신뢰 경계) 의 유지 - llm은 의미 공간이기 때문에 컨텍스트 안의 텍스트를 구조적으로 "지시" 와 "데이터"로 구분하지 못하기 때문에 신뢰 수준을 다르게 적용해서 신뢰 경계를 유지해야한다.
2. least privilege(최소 권한원칙)의 실현 에이전트가 여러 도구들과 연결되어 있을때 그 도구들의 작업에는 필요한 작업에만 접근하도록 해야하기때문에 분리해야한다.
3. 세션 - 테넌트 간 데이터 경계 -mcp서버에서 다른 사용자들의 데이터가 유출될수 있다.

###  컨텍스트가 분리되지 않았을 때 발생할 수 있는 보안·권한·데이터 오염 문제를 구체적으로 분석하시오.

**1. 간접 프롬프트 인젝션 (Indirect Prompt Injection)**

외부의 데이터 소스를 가져와서 처리하는 경우 프롬프트의 사용자 요청과 외부 데이터 간에 컨텍스트 격리가 없다면 프롬프트와 데이터를 구분하지못하고 데이터에 있는내용을 요청으로 처리하거나 악의적으로 데이터에 중요한 데이터를 강제로 특정 url로 보내라는 데이터를 넣어놓는다면 에이전트가 중요한 정보를 유출하게 될 수도 있다.

**2. Confused Deputy 문제 (권한 혼동)**

에이전트는 사용자를 대리해 여러 자격 증명(토큰, 세션)을 보유합니다. 컨텍스트 격리가 없으면 "누구의 권한으로, 누구의 요청에 의해" 도구를 호출하는지가 흐려집니다. 악의적 MCP 서버 하나가 응답 데이터에 지시를 심어, 에이전트가 보유한 다른 서버의 고권한 자격 증명을 대신 행사하게 만들 수 있습니다. OAuth 토큰이 컨텍스트에 노출되면 토큰 자체가 유출될 수도 있습니다.

**3. 크로스 서버 데이터 유출 (Cross-server exfiltration)**

민감한 사내 DB 조회 결과가 컨텍스트에 남아 있는 상태에서 외부 SaaS 도구를 호출하면, 모델이 도구 호출 인자에 그 민감 데이터를 포함시킬 수 있습니다. 격리가 없으면 "내부 데이터는 외부 도구로 나가면 안 된다"는 데이터 흐름 정책을 강제할 지점이 없습니다.

**4. Tool Poisoning / 도구 설명 오염**

MCP 서버가 제공하는 도구 설명(description) 자체에 악성 지시를 심는 공격입니다. 도구 메타데이터가 시스템 프롬프트급 신뢰로 컨텍스트에 주입되면, 서버 하나만 악성이어도 전체 에이전트 동작이 오염됩니다. 특히 서버가 나중에 설명을 몰래 바꾸는 "rug pull" 형태도 가능합니다.

**5. 컨텍스트 오염과 판단 왜곡 (Data/Context Pollution)**

보안 공격이 아니더라도, 저품질·오래된·상충하는 데이터가 여러 소스에서 뒤섞이면 모델이 출처별 신뢰도를 구분하지 못해 잘못된 데이터를 근거로 결정을 내립니다. 예: 테스트 DB의 더미 데이터가 운영 DB 데이터와 섞여 잘못된 리포트를 생성하거나, 이전 작업의 잔여 컨텍스트가 다음 작업에 영향을 주는 상태 누수(state leakage)가 발생합니다.

**6. 세션 간 유출과 멀티테넌시 붕괴**

MCP 서버가 세션별 컨텍스트를 분리하지 않으면 사용자 간 대화 내용, 조회 결과, 자격 증명이 교차 유출됩니다. 이는 규제 관점(개인정보보호법, GDPR 등)에서도 직접적인 컴플라이언스 위반입니다.

