리스트 - 데이터를 순차적으로 저장하여 가독성을 높이고 편리하게 관리하기 위해
(순서대로) - 순서가 있는것이다 . 
리스트의 고유명사 - 유동적으로 크기를 바굴 수 있는 순차적 형태의 자료구조
list = [1,2,3,4]
리스트 조회 
list = [1,2,3,4]
print(f"첫번쨰 list 는 {list[0]} 이다")

리스트 요소 추가
list.append(5)
리스트 요소 변경
list[4] = 6
리스트 요소 삭제
del list[3]
리스트 마지막 요소 삭제
list.pop() 
리스트 첫번째 요소 삭제
list.pop(0)

함수(수학) - 두개의 변수 x,y에서 , x가 일정한 범위 내에서 값이 변하고 y의 값이 정해질때,x에 대하여 y를 이르는 말 
(정보통신) - 재활용하기위해 묶어놓은 코드들의 집합
def make_bread(a,b):
	print("빵빵")
	return "빵"
	
	
	
	
	
$$
y = (x|x\in )
$$